[Cursor] Local dev: replace Replit API with localhost, unify axios usage, add Mongo setup notes

- Point all frontend requests to http://localhost:3000 via shared src/lib/api.js
- Refactor temp pages to import shared api instead of per-page axios instances
- Remove remaining hardcoded replit.dev URLs
- Update Frontend/README.md with guidance
- Add notes in .cursorrules and task plan

Backend notes:
- Ensure backend/.env (gitignored) with MONGO_URI, JWT_SECRET, PORT
- Start MongoDB locally (brew or Docker) to resolve ECONNREFUSED 127.0.0.1:27017
